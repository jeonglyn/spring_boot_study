* 나만의 검증 규칙 만들기

@Valid, @Validated 정리

- @Valid       : 표준 Bean Validation / 자바 표준 검사 도구 / 컨트롤러에서 주로 객체 값을 검사할 때 사용 / 검사할 객체 앞에 붙여서 사용
- @Validated   : 스프링이 제공하는 검사 도구 / 스프링 AOP를 사용하여 계층 상관없이 검사 가능 / 그룹별로 다른 검사 규칙을 적용할 때 주로 사용
  -> @Validated(Create.class)을 이용하여 그룹 지정이 가능
  -> @RequestParam, @PathVariable 를 활용하여 클래스 레벨에서도 사용이 가능


* Custom Constraint(나만의 검증 로직)가 필요한 이유
- @NotBlank, @Min, @Email 같은 기본 제약 어노테이션으로는 표현 못하는 도메인 규칙이 있음
  예를 들어, 비밀번호와 비밀번호 확인은 같아야 한다는 점이나 비밀번호는 영문+숫자 조합이어야 한다 라는 규칙

  이럴 때 직접 어노테이션 + ConstrainValidator 구현체를 만들 수 있다.


* 중첩 검증 (Nested Validation)
- 하나의 클래스(DTO)안에 또 다른 클래스(객체나 리스트)가 포함되어 있을 때 발생한다.
- 문제점 : 그냥 검증을 돌리면 겉에 있는 데이터만 검사하고, 안쪽에 들어있는 객체나 리스트 내부 데이터는 검사하지 않고 무시한다.
- 해결법 : 안쪽에 있는 객체나 리스트 필드 앞에 반드시 @Valid 를 명시적으로 붙여줘야 안쪽까지 파고들어 검사한다.


* 에러 메시지 처리
- GlobalExceptionHandler 는 여러 검증 에러 중 첫 번째 에러 1개만 응답에 담아 반환하는 구조이다.
- 실무에서는 클라이언트가 여러 입력창의 에러를 한 번에 다 보여줘야 하는 경우가 많아서, 발생한 모든 필드별 에러를 모아서 한번에 응답해주는 방식을 사용한다.


* 주요 어노테이션
- @Target, @Retention, @Constraint