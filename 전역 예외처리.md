* 전역 예외처리
- 전역 예외처리가 필요한 이유는?
    => Spring이 기본 제공하는 에러 페이지(또는 기본 JSON)는 클라이언트에게 그대로 노출될 가능성이 크다.
        -> 1) 일관성이 없다 : 컨트롤러마다 try-catch를 따로 작성하면 같은 종류의 에러인데도 응답 형태가 제각각
        -> 2) 정보 누출 위험 : 기본 에러 응답에는 스택 트레이스나 내부 클래스 이름 같은게 그대로 담길 수 있다.

- 그래서 컨트롤러 코드 밖에서, 애플리케이션 전체 예외를 한 곳에서 잡아 일관된 형식으로 변환해주는 계층이 필요하다!
- 이게 바로 @ControllerAdvice + @ExceptionHandler 조합이다.


* 동작 원리
- @RestControllerAdvice가 붙은 클래스는 모든 @RestController에서 발생하는 예외를 가로챌 수 있는 역할을 함
- 그 안에서 @ExceptionHandler가 붙은 메서드가 해당 예외 타입을 처리한다.
- 이로써 컨트롤러는 예외 처리를 신경 쓰지 않고, 비즈니스 로직에만 집중할 수 있게 되는것이다!

