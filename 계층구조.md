* 계층 구조 & 패키지 설계

* 계층을 나누는 이유

한 클래스에 HTTP 처리, 비즈니스 로직, DB접근을 다 넣으면 코드 수정 시 영향 범위를 예측하기 어려움!  
그래서 각 계층별로 역할(책임)을 나누어 설계를 해야한다
Controller   => HTTP 요청/응답 처리, 입력 검증, Service 호출
Service      => 비즈니스 로직, 트랜잭션 관리, DTO -> Entity로 변환
Repository   => 데이터 접근 (DB CRUD)
DTO          => 계층 간 데이터 전달, Entity 노출 방지


* 패키지 구조 예시
com.study.springboot.member
  -- Controller
        -- xxxController.java
  
  -- Service
        -- xxxService.java

  -- Repository
        -- xxxxxRepository.java
        -- xxxxxRepositoryImpl.java (지금은 메모리 저장! 나중에 JPA 공부 진행예정)
  
  -- Domain
        -- xxxx.java

  -- DTO
        -- xxxxxDto.java

