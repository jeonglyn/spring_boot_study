* 스프링 시큐리티
- 스프링 시큐리티란?
  => 누가 요청을 보냈는지 확인하고(인증), 그 사람이 이 요청을 처리할 자격이 있는지 판단(인가) 해주는 일을 하는 프레임워크
  => 스프링 시큐리티를 사용하지 않는다면, if(로그인 안했으면..) return 401; 이거처럼 모든 경우의 수를 코드로 작성해야함..


- 핵심 개념
  => 인증(Authentication)과 인가(Authorization), 필터 체인(Filter Chain)

  => 인증 : 사용자(클라이언트)가 애플리케이션의 특정 동작에 관하여 허락된 사용자인지 확인하는 절차
    - 1) 크리덴셜 기반 인증 방식 : 사용자명, 비밀번호로 인증하는 전통적인 방식
    - 2) 이중 인증 방식         : OTP와 같이 추가적인 인증 방식을 도입해 한번에 2가지 방법으로 인증하는 방식
    - 3) OAuth2 인증 방식      : 소셜 미디어를 사용해 편리하게 인증하는 방식으로 최근에 필수적으로 쓰이고 있음

  => 인가(권한부여) : 데이터나 프로그램 등의 특정 자원이나 서비스에 접근할 수 있는 권한을 허용하는 것을 말함
  => 필터 체인     : 사용자의 요청이 프로그램에 닿기 전에 보안 규칙을 먼저 확인하는 통로
    - 요청이 들어왔을 때 실제로 벌어지는 일
  
                클라이언트 요청
                       │
                       ▼
    ┌─────────────────────────────────────┐
    │     Security Filter Chain           │  ← 스프링 시큐리티가 만든 "문지기" 필터들
    │   (컨트롤러 도달 전에 먼저 거침)         │
    │                                     │
    │   1) 요청에서 인증 정보 추출            │  (예: 헤더의 JWT 토큰)
    │      (지금은 없음 → 5.x부터는          │
    │       개발자가 직접 필터를 끼워넣음)     │
    │   2) 그 정보로 "누구인지" 판별          │
    │   3) SecurityContext에 저장          │  (이후 컨트롤러/서비스 어디서든
    │                                     │   "현재 로그인한 사용자"를 꺼내 쓸 수 있음)
    │   4) 요청 경로 + 권한을 비교해서        │
    │      통과시킬지 막을지 결정             │
    │                                     │
    └─────────────────────────────────────┘
                       │  (통과한 경우만)
                       ▼
                   Controller